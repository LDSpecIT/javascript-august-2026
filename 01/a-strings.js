
// variables
// var, const, let --> var is older model that may not fit all framework code. Const is a constant that may not be changed in many times of its use. let is a variable condition call to a variable that can be changed.

var numberOfHotDogsEaten = 15
// This can be changed in console

let numberOfPizesWon = 10
// This can be changed by console

const nameOfThemePark = "Coney Island"
// This can not change on console


// STRINGS

// text

const firstString = `A ton of text`
const useOfQuotes = `I understand how to use \"quotes\" inside quotes`

// interpolation --> let inside const changing as line changes
let lineLength = 6
const withBacktics = `Current Line Queue: ${lineLength}`

// CONCATENATION this is one way to bring strings together

const thingOne = "one"
const thingTwo = "two";
const thingThree = thingOne + thingTwo;


// create two values


// Using opening openTime and closeTime - create a prompt to let visitors known operating hours: Make sure that if you change the times constants, the strings will follow the change.

const openTime = "7am";
const closeTime = "12am";

const operatingHours = `Our hours of Operation are ${openTime} through ${closeTime}`



// Use Concatenation or Interpolation to create your full name.


const firstName = "Luciano"
const lastName = "DeSouza"

const userFullname = firstName + " " +  lastName;



